= form_for @outfit do |f|
  -if @outfit.errors.any?
    #error_explanation
      %h2= "#{pluralize(@outfit.errors.count, "error")} prohibited this outfit from being saved:"
      %ul
        - @outfit.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
